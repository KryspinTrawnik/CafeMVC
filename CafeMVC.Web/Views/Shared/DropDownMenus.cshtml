@model List<CafeMVC.Application.ViewModels.Menu.MenuForListVm>

@{
   
}
<div class="dropdown">
    <button class="dropdown-toggle">Menu</button>
    <ul class="dropdown-menu">
        @for(int i =0; i <Model.Count; i++)
        {
            <li> @Html.ActionLink(Model[i].Name, "ViewMenu", "Menu", new{menuId = Model[i].Id})  </li>
            
        }
        
    </ul>
</div>